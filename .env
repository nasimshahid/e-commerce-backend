PORT=4000
MONGODB_URI=mongodb://localhost:27017/ecommerce

# ⚠️  IMPORTANT: Use a strong random secret in production.
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_SECRET_BEFORE_PRODUCTION

NODE_ENV=development

# Frontend URL — used for CORS whitelisting
FRONTEND_URL=http://localhost:3000

# Admin Setup Key — required to call POST /api/v1/create-admin
# Set this to a strong secret and keep it private
ADMIN_SETUP_KEY=CHANGE_THIS_TO_A_STRONG_SETUP_KEY

# Email Configuration
# ⚠️  IMPORTANT: The previous App Password was committed — rotate it immediately at:
# https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=snasim1786@gmail.com
EMAIL_PASSWORD=pmujgadapmriwrll 
EMAIL_FROM=noreply@ecommerce.com
